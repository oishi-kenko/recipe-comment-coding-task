# このファイルをコピーして `.env` という名前のファイルを作り、
# 別途共有される OpenAI API キーを下記の右辺に貼り付けてください。
#
#   cp .env.example .env
#
# `.env` は .gitignore で除外されており、Git にはコミットされません。
# （API キーを誤って公開リポジトリに push しないための仕組みです）

OPENAI_API_KEY=ここに共有されたAPIキーを貼り付けてください

# 使用するモデル名（変更したい場合のみ編集してください）
OPENAI_MODEL=gpt-4o-mini
