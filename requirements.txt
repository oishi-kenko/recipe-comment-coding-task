# 本課題で使用するライブラリとバージョンです。
# `pip install -r requirements.txt` でまとめてインストールできます。
# 必要に応じてライブラリを追加した場合は、このファイルにも追記してください。

fastapi==0.136.3
uvicorn[standard]==0.48.0
openai==2.38.0
python-dotenv==1.2.2
pydantic==2.13.4

# テスト用
pytest==9.0.3
httpx==0.28.1
